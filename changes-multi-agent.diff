--- 1start-mathmodel/SKILL.md (before)
+++ 1start-mathmodel/SKILL.md (after)
@@ -1,6 +1,6 @@
 ---
 name: 1start-mathmodel
-description: "数学建模竞赛工作流入口。用于启动完整建模流程：询问用户偏好，生成 plan.md 和 todo.md，并按阶段调用赛题分析、建模、代码与图表、流程图、论文撰写、验证验收等 skills。"
+description: "数学建模竞赛工作流入口。用于启动完整建模流程：沿用或补齐用户偏好，生成 plan.md 和 todo.md，按阶段完成分析、建模、代码、图表、论文和验收，支持按需多 agent 方案比选与独立复核。"
 allowed-tools: Bash(*), Read, Write, Edit, Grep, Glob, Agent, WebSearch, WebFetch
 ---

@@ -18,6 +18,18 @@

 - `plan.md`：整体流程方案、建模方向、阶段顺序、预期产物和风险控制。
 - `todo.md`：具体待办事项列表，记录每个阶段的任务和状态。
+
+## 按需多 agent 协作
+
+主 agent 负责阶段调度、文件整合、计划更新和返工。协作设置沿用用户最新明确指示或 `plan.md`，未指定时使用 `auto`，无需为默认设置再次提问：
+
+- `single`：不创建子 agent，由主 agent 执行。
+- `auto`：在 `2analysis-modeling` 存在关键假设、候选方法分歧或可独立核验的数据问题，或在 `6verity` 完整验收中存在可独立分工的审查范围时，按需委派。简单修改与依赖尚未确定的任务保持单 agent。
+- `multi`：在上述两个阶段使用真实子 agent，人数与任务相匹配。默认最多同时 3 个子 agent，受用户设置和宿主实际并发限制约束。
+
+进入上述阶段前，主 agent 按需读取 [协作协议与角色职责](references/multi-agent.md)，使用实际可用的子 agent 工具执行委派，等待所需结果并按证据整合。第一版保持阶段顺序，`3coding-visual`、`4drawio`、`5writing` 仍由指定负责人顺序执行。子 agent 只执行自己的任务包，不递归招募或重新启动完整工作流。
+
+工具不可用、并发资源不足或部分任务失败时，按协作协议记录真实执行方式并继续可完成的工作；不得把单 agent 的多角色叙述记为独立复核。子 agent 自报完成不能代替主 agent 检查产物与验收证据。

 ## 工作流

@@ -53,6 +65,12 @@
 - 竞赛类型：<国赛 / 华为杯 / MCM / ...>
 - 论文语言：<中文 / 英文>
 - 子问题数量：<已知 N 个 / 待分析确定>
+
+协作设置：
+- 模式：<auto / single / multi，默认 auto>
+- 最大并行子 agent 数：<默认 3，按宿主可用并发下调>
+- 启用阶段：2analysis-modeling、6verity
+- 本轮实际分工与结果记录：<按需填写任务、角色、输入版本、输出路径、状态及降级原因>

 workflow:
    step      skills
@@ -107,7 +125,7 @@
 - [ ] 5. 验证和验收 - `6verity`
 ```

-每完成一个阶段，都要更新 `todo.md` 中对应任务的状态。
+每完成一个阶段，都要更新 `todo.md` 中对应任务的状态。协作阶段由主 agent 汇总所需子任务并核对产物后再勾选；子任务输出按需保存在 `reports/agents/<阶段>/<轮次>/<任务>/`，总报告由主 agent 统一维护。

 ### 4. 依次执行阶段

--- 2analysis-modeling/SKILL.md (before)
+++ 2analysis-modeling/SKILL.md (after)
@@ -20,6 +20,14 @@

 不要在本阶段写论文正文，不要生成最终 `paper/`，不要把图表排版任务提前到这里。

+
+## 按需专家协作
+
+协作设置沿用会话或 `plan.md`，未指定时默认 `auto`。主 agent 读取 [协作协议](../1start-mathmodel/references/multi-agent.md)，先明确题面事实、子问题范围和输入文件，再决定是否委派。关键假设有歧义、候选方法存在实质差异或数据适用性需要独立审查时，可招募数据审查、模型方案、备选方案或反例检查专家，按实际需要选择，最多同时 3 个子 agent，并服从宿主可用并发限制。
+
+给专家提供同一版本的原始题面和数据，先独立产出依据、适用条件及检验建议；已有方案仅在针对性审查时作为待检验对象提供，不把总控预设结论当成答案。方法比选必须围绕统一目标、约束和评价口径。子任务不是新的赛题顶层问题，不改变本 skill 的子问题计数规则。
+
+主 agent 汇总专家报告，在 `reports/ANALYSIS_MODELING_REPORT.md` 中增加按需的“方案比选与分歧处理”小节，记录采用与未采用方案、证据、尚未解决的假设和待验证项。专家的小样本验算只作可行性依据，最终数值由 `3coding-visual` 生成。子 agent 不直接改写总报告或生产代码；角色、输入与独立输出目录遵循协作协议。单 agent 模式直接完成下列流程，不虚构专家讨论记录。

 ## 工作流程

--- 6verity/SKILL.md (before)
+++ 6verity/SKILL.md (after)
@@ -31,6 +31,16 @@
 7. 编译后的 PDF，或可由入口文件编译得到的输出 PDF。

 不要假设论文目录一定叫 `paper/`，也不要假设结果文件一定在项目根。若项目使用不同命名，按实际结构传参并在 `reports/VERIFY_REPORT.md` 中说明。
+
+## 按需独立复核
+
+协作设置沿用会话或 `plan.md`，未指定时默认 `auto`。主 agent 读取 [协作协议](../1start-mathmodel/references/multi-agent.md)。完整验收中可独立分工时，按需要招募数学与数值一致性、代码复现、格式与引用审查专家；优先使用未参与被审查产物编写的新子 agent，并记录其审查范围和独立性。局部、简单的修正可保持单 agent。
+
+先确定本轮被审查文件的版本，并明确每位专家对应下列哪些检查。子 agent 只读生产产物，只将报告、复现输出或编译副本写入分配的独立目录；真实编译和复现在副本中进行，保留项目相对布局，不覆盖正式 PDF、结果或日志。多个专家不得同时运行同一套检查或修改同一份文件。未运行的检查如实标为 `NOT_RUN`，不得通过意见汇总补成 `PASS`。
+
+主 agent 等待所需审查结果，核对关键问题的文件定位、命令和证据，合并到唯一的 `reports/VERIFY_REPORT.md`。修改被审查产物后，旧版本相关结论失效，应按 Step 10 复验。`single` 或自动降级时如实说明未进行独立复核；若用户明确要求的独立复核无法完成，将该必需检查列为 `NOT_RUN`，按 Step 9 判为 `BLOCKED`（已有硬错误时仍为 `FAIL`）。
+
+以下修复指令由主 agent 或其指定的前序阶段负责人执行；审查子 agent 返回发现与建议，不在审查过程中修改生产产物。

 ## 工作流程

@@ -205,6 +215,9 @@

 提交就绪：是 / 否（仅总体结论为 PASS 时为“是”）

+## 协作与独立性
+实际模式、审查者与负责范围、输入版本、报告位置、未完成任务及降级原因。
+
 ## 检查项
 | 检查项 | 结果（PASS / FAIL / WARN / NOT_RUN） | 证据或未执行原因 |
 | --- | --- | --- |
--- 1start-mathmodel/references/multi-agent.md (before)
+++ 1start-mathmodel/references/multi-agent.md (after)
@@ -0,0 +1,80 @@
+# 数学建模多 agent 协作协议
+
+本文件由 `1start-mathmodel`、`2analysis-modeling` 和 `6verity` 按需读取。它只负责阶段内分工，现有阶段产物与验收规则继续生效。
+
+## 启用与分工
+
+- 用户最新明确指示优先，其次为 `plan.md` 的协作设置；缺省 `auto`，最大同时 3 个子 agent。始终按宿主真实可用并发下调，人数由互补任务决定，不为凑人数创建角色。
+- `single` 不创建子 agent。`auto` 在建模阶段有重要假设、候选模型分歧、独立数据检查，或完整验收有不同审查范围可分工时委派。简单修改、只有单一紧耦合步骤时由主 agent 完成。
+- `multi` 在建模和验收阶段执行真实委派；若无法建立独立任务或工具不可用，先记录原因并完成可执行部分，不伪造多 agent 执行。
+- 只有主 agent 招募、分配、等待和汇总。作为已委派子 agent 读取本文件时，只处理任务包，不再招募、不重新运行整个 skill。遇到职责外问题返回主 agent。
+- `3coding-visual`、`4drawio`、`5writing` 由明确负责人顺序执行；这一版的多 agent 不跨越阶段依赖。
+
+## 动态角色
+
+按任务选择互补职责，角色可以复用或合并，但同一产物的作者不得被记为独立审查者。
+
+| 阶段与角色 | 负责内容 | 返回证据 |
+| --- | --- | --- |
+| 建模：数据与题意审查 | 单位、变量可用性、缺失、歧义、训练验证隔离、约束来源 | 题面或字段定位、可用与不可用输入、影响模型的未知信息 |
+| 建模：模型方案 | 按题型提出公式、目标、约束、求解与校验路径 | 明确假设、可实现模型、适用条件与局限 |
+| 建模：备选方案或反例检查 | 提出有实质差异的方案，或检查模型边界、不可行情形与假设敏感性 | 方案差异、反例、小规模验算与需要进一步验证的指标 |
+| 验收：数学与数值一致性 | 题目覆盖、模型公式、约束、结果与论文关键数值对应关系 | 具体文件位置、计算核对与冲突清单 |
+| 验收：代码复现 | 运行条件、核心实现与报告一致性、结果可复现性 | 实际命令、退出状态、输出对照、未执行原因 |
+| 验收：格式与引用 | 文本门禁、章节、图片、引文、编译和逐页视觉检查 | 文件或页面位置、脚本与编译日志、真实视觉检查结果 |
+
+建模方案先独立分析同一份原始材料，再交换与具体分歧有关的证据。独立审查优先使用新子 agent，仅提供原始任务、待审产物和检查要求，不告知预期 PASS/FAIL，也不把其他审查者结论当作已证实事实。
+
+## 委派任务包
+
+每次真实委派前明确以下信息，可用简洁文本，不强制 JSON：
+
+- 任务编号、所属阶段、角色和要回答的具体问题。
+- 输入文件绝对路径及本轮版本标识（如内容哈希或固定副本），共同的题面事实、单位、约束和评价口径。
+- 依赖关系与所需完成条件；等待前序结论的工作不提前执行。
+- 允许的操作、允许写入的确切目录、不能改动的共享产物。
+- 输出报告位置，以及结论所需的文件定位、运行记录或推导依据。
+- 本任务适用的时间、计算预算与中止条件；没有授权或无法执行的动作返回原因，不自行扩大任务。
+
+主 agent 记录宿主返回的真实子 agent 标识与任务的对应关系。只采用宿主实际存在的创建、发送任务、等待、读取结果工具；不要假设 `Agent` 或某个函数名在所有环境中都存在。只有角色提示词、串行自问自答、没有真实创建成功的调用，不算独立 agent 执行。
+
+## 产物与写入归属
+
+- 原始题面、原始数据和本轮已确定的上游产物供所有专家只读。主 agent 为每位专家分配独立的 `reports/agents/<阶段>/<轮次>/<任务>/` 目录，报告及临时验算文件均写入该目录。
+- `plan.md`、`todo.md`、`reports/ANALYSIS_MODELING_REPORT.md` 和 `reports/VERIFY_REPORT.md` 由主 agent 唯一写入。子 agent 返回报告或修改建议；主 agent 审核后整合。生产代码、结果、图表、论文仍由各阶段负责人维护。
+- 审查子 agent 如需编译或复现，使用任务目录中的项目副本，保留原始相对布局，明确来源版本；不覆盖正式结果或 PDF。超出任务包的写入、依赖安装或环境修改交回主 agent，原有权限边界不变。
+- 主 agent 在同一轮审查期间不修改正在被审查的文件。必须修改时，通知相关专家停止或重取输入，将旧版本相关结果标为过期，再复验。
+
+## 返回格式
+
+每位专家返回可检查的摘要，并在已分配目录保存完整报告：
+
+```text
+任务编号 / 角色 / 输入版本：
+任务状态：完成 / 部分完成 / 阻塞 / 失败
+结论与适用范围：
+证据：文件位置、关键推导、实际命令与输出
+发现的问题或备选方案：
+不确定性、未执行项及原因：
+产物绝对路径：
+需要主 agent 处理的事项：
+```
+
+任务“完成”只表示分配的工作完成，不等同于论文验收 PASS。未实际运行的实验、编译、视觉检查均应说明；专家置信表达不能代替证据。
+
+## 汇总、分歧与复验
+
+1. 主 agent 等待所有必要任务返回，核对原题覆盖、职责是否重复、输入版本、证据与输出文件。边等待边做不冲突的整合准备，避免重复执行已委派任务。
+2. 比较模型时使用相同数据口径、约束和指标。先区分互补结论与真正冲突，再把未解决的分歧转为边界验算、敏感性分析或对照实验任务，写明什么结果会改变选择。
+3. 优先采用可追溯题面、数学证明、实际约束验证和可复现实验结果。多数票和专家人数不决定结论；证据不足时保留不确定性，不把多个同模型角色的一致表态当成独立证实。
+4. 默认完成一轮独立分析与合并；只有具体未决问题需要新证据时，追加针对性任务。没有新增证据来源或预算不足时结束讨论，记录未决项与下一步，不机械执行固定轮数辩论。
+5. 建模结论整合到分析报告，最终数值仍交给代码阶段。验收结论按 `6verity` 的 FAIL 优先、必需检查未执行则 BLOCKED、实际通过才 PASS 的规则汇总，不平均专家投票。
+6. 修复生产产物交给原阶段负责人；主 agent 更新计划、待办与受影响检查，只复用对应产物未变化的既有证据。
+
+## 超时、失败与能力降级
+
+- 并发名额不足时使用更小批次或顺序启动真实子 agent，尊重宿主上限；不能靠递归招募绕过限制。
+- 子 agent 失败或超时，先检查已留下的产物，记录失败与覆盖缺口。可更换 agent 或由主 agent 接管剩余工作；接管后只能标为主 agent 检查，不能标为独立复核。
+- `auto` 下工具不可用时退回单 agent，记录实际模式及原因，继续原有检查；这本身不把其余已完成工作判失败。
+- 用户明确要求 `multi` 或独立复核，而真实委派/独立检查未完成时，明确记录该要求尚未满足。验收中将所需独立复核列为 `NOT_RUN`，总体按 `6verity` 规则处理；建模中记录部分完成及待补审查，不声称已完成要求。
+- 模式、人员、输入版本、输出位置、分歧处理与降级原因进入现有计划或阶段报告；不把协作过程和内部路径写入最终竞赛论文。
