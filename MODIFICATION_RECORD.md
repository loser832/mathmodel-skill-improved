# Mathmodel skills 修改记录

最近更新：2026-09-06。状态：结构化交叉质询规则已完成、验证并同步到实际 skill 目录；此前按需多 agent 协作及两轮演练继续作为回归基线。

## 本轮：结构化交叉质询（2026-09-06）

目的：在保留独立首稿和证据仲裁的前提下，为真正未决的关键冲突增加可执行的一次定向质询与一次强制回应，避免固定轮次自由辩论、机械投票和无意义的通信扩张。

- `1start-mathmodel/SKILL.md`：补充质询触发路由及 `plan.md` 记录项。
- `1start-mathmodel/references/multi-agent.md`：集中规定首稿冻结、触发门槛、最多 3 个命题、单向一次质询、三态回应、可证伪检查、证据仲裁、停止条件、版本失效和验收独立性。
- `2analysis-modeling/SKILL.md`：明确独立性只约束首稿形成，冻结后允许证据交换和互补方案整合，并记录质询与立场变化。
- `6verity/SKILL.md`：区分独立复核与作者自检，禁止质询把 `NOT_RUN` 补成 `PASS`，修复后由未参与修复的审查者复验。

候选快照和实际安装目录的四个文件 SHA-256 已逐一一致；三个相关 skill 在两个位置均通过 `quick_validate.py`。独立行为审查覆盖一个必须触发的题意歧义场景和一个不得触发的互补分工场景，均通过。详细证据见 [结构化交叉质询验证报告](validation/CROSS_EXAM_REVIEW.md)。机器相关的安装回执仅保留在本地维护档案中。

本轮只更新四个协作逻辑文件，没有改动脚本、模板或其他 skill。修改前备份保留在本地维护档案中，不纳入公开仓库。

## 仓库结构

- 维护档案：本仓库根目录。
- 完整工作流快照：`skills/`，包含入口、建模、编程图表、DrawIO、写作、验收与共享规范七个目录。
- 实际使用时，将对应目录安装到用户级 `.agents/skills/` 目录。
- 修改前备份、路径清单和安装回执仅保留在本地维护档案中。
- 本轮逐行差异：[changes-multi-agent.diff](changes-multi-agent.diff)。

## 前一轮：按需多 agent 协作（2026-09-04）

目的：在建模阶段引入有独立依据的方案与反例检查，在验收阶段引入与作者分离的复核；继续使用原有阶段产物、偏好继承和返工规则。

| 文件 | 修改 | 作用 |
| --- | --- | --- |
| `1start-mathmodel/SKILL.md` | 修改 | 添加协作模式、调用条件、阶段限制和计划中的执行记录；总控核对所需任务后才能完成阶段待办。 |
| `2analysis-modeling/SKILL.md` | 修改 | 按题意歧义和方法差异选择专家；同源输入、先独立分析，再按证据比选；子任务不增加赛题顶层问题数。 |
| `6verity/SKILL.md` | 修改 | 划分独立复核范围，隔离编译和复现副本，记录真实审查者与未执行项；总控按原有判定规则合并结果。 |
| `1start-mathmodel/references/multi-agent.md` | 新增 | 集中规定任务包、角色、并发、写入归属、证据输出、分歧处理和能力降级。 |

协作方式：

- 默认 `auto`；已有会话或 `plan.md` 中明确设置继续沿用。简单任务保持单 agent，复杂建模和可分工的完整验收按需委派，无需为默认值重复提问。
- `single` 不启动子 agent；`multi` 在建模与验收阶段执行真实委派。默认最多同时 3 个子 agent，并按宿主可用并发和任务数量下调。
- 第一版只接入 `2analysis-modeling` 和 `6verity`。代码、图表、论文仍按阶段依赖顺序执行。
- 主 agent 单独维护 `plan.md`、`todo.md` 和阶段总报告；子 agent 只向其独立任务目录写入。审查期间冻结待审输入，复现和编译使用保留相对布局的副本。
- 记录真实子 agent 标识、输入版本、输出文件和证据；禁止把角色扮演写成真实多 agent 复核。子 agent 不递归招募。
- 根据推导、反例、约束验证和可复现实验处理分歧，不按票数表决，不设置机械的固定辩论轮次。
- 自动模式下能力不足可如实降级；用户明确要求的独立复核未完成时保留未完成状态。验收继续遵循：已有硬错误为 `FAIL`，无硬错误但必需检查未执行为 `BLOCKED`，实际检查满足要求才为 `PASS`。

## 验证

静态检查已通过：三个修改的 SKILL.md 均通过 skill-creator 格式校验，三个新协议链接均可解析；相对备份仅修改预期三个已有文件并新增一个协议文件；前两轮的用户偏好和验收返工规则保留。详见 [static-results.json](validation/static-results.json)。

真实 agent 行为验证已完成：

| 演练 | 实际协作 | 观察结果 |
| --- | --- | --- |
| 建模歧义与可行性 | auto；两名真实独立专家；总控合并 | 识别拆单与单源解释的差异、增长后的容量矛盾；保留两问结构和已有偏好；五项输入哈希未变。 |
| 独立验收与状态合并 | multi；两名真实独立审查者；总控负责格式 | 实际复现返回 20、论文却写 23；正确判定样例 FAIL，编译及视觉检查保持 NOT_RUN，原始八项输入未改动。 |

详见 [FORWARD_TEST_REPORT.md](validation/FORWARD_TEST_REPORT.md) 和 [behavior-results.json](validation/behavior-results.json)。演练中样例的 FAIL 是预期发现错误，表示验收行为有效；不是 skill 安装失败。

合成场景不代表完整竞赛论文已经通过验收，也不构成质量或成本优势的对照实验。没有实际演练所有失败/超时/降级分支。已有捆绑 MiKTeX 前序启动检查提示初始化及配置目录问题，本次不安装或初始化环境；真实论文编译与视觉验收仍需在具体项目中完成。

## 已归档的前两轮修改

### 第一轮：偏好只询问一次

修改入口和写作 skill：优先遵循用户最新明确指示，其次沿用 `plan.md`；仅缺失或无法消解的冲突才询问。统一排版引擎及论文语言规则，保留用户明确选择。

原始备份、清单及逐行差异已保存在本地历史档案中，公开仓库仅保留改进后的工作流快照和汇总记录。

### 第二轮：图片路径与验收返工

- LaTeX 图片路径统一按论文主入口的编译目录解析；Typst 仍按引用文件位置解析。写作规范与验收脚本保持一致。
- 编译固定工作目录和入口，记录实际执行结果；未执行的编译或视觉检查不能算通过。
- 加入 `PASS / FAIL / BLOCKED` 汇总和前序阶段返工映射；修复后重新检查受影响项，不凭报告文件存在就完成待办。
- 当轮八个完整检查脚本场景通过，技能格式与 Bash 语法检查通过；没有完成真实论文编译，环境限制已记入原记录。

第二轮的完整备份、安装校验回执、差异和验证材料保存在本地历史档案中；本节保留公开所需的变更摘要。

## 恢复与范围

本地维护备份是前两轮优化完成后的基线。若只回退多 agent 功能，应从本地备份恢复本轮修改的三个 SKILL.md，并移除本轮新增的协作协议；无需回退偏好、图片路径或验收返工修复。先对照本地清单核对文件，避免覆盖后续改动。本记录不自动执行回退。

`skills/` 是此次维护的工作流快照，外部技能和运行时仍使用各自安装版本。公开仓库保留差异、合成验证输出和修改记录；机器相关备份、路径清单与安装回执不发布。
